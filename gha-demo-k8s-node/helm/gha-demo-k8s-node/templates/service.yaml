apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-k8s-node.fullname" . }}
  labels:
    {{- include "sample-k8s-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-k8s-node.selectorLabels" . | nindent 4 }}
