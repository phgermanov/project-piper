apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-k8s-node.fullname" . }}-test-connection"
  labels:
    {{- include "sample-k8s-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-k8s-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
